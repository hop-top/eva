name: echo_agent
provider: echo-agent
request_schema:
  type: object
  required: [message]
  properties:
    message:
      type: string
evaluators:
  - name: contains
    mode: binary
retry_policy:
  max_retries: 2
  hint: "Respond with the original message echoed back"
  backoff_ms: 100
